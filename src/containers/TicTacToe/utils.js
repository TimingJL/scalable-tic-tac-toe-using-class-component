

export const getWinner = (blocks, winnerCondition) => {
    let winner;
    let winCaseArr = [];
    let isGameFinished = false;
    return {
        winner,
        winCaseArr,
        isGameFinished,
    };
}
